{
  "name": "full-stack-cen-template",
  "private": true,
  "type": "module",
  "packageManager": "npm@10.9.8",
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "build": "npm --prefix frontend run build",
    "check": "npm run check:generated && npm run typecheck && biome check . && npm run typecheck:backend && npm run lint:backend && npm run format:backend:check",
    "check:generated": "node scripts/check-generated.mjs",
    "db:migrate": "uv run --project backend alembic -c backend/alembic.ini upgrade head",
    "db:revision": "uv run --project backend alembic -c backend/alembic.ini revision --autogenerate",
    "dev": "node scripts/dev.mjs",
    "fix": "biome check --write . && uv run --project backend ruff check --fix backend && uv run --project backend ruff format backend",
    "format:backend:check": "uv run --project backend ruff format --check backend",
    "generate-client": "node scripts/generate-client.mjs",
    "lint:backend": "uv run --project backend ruff check backend",
    "test": "npm run test:scripts && npm run test:deploy && npm run test:backend",
    "test:backend": "uv run --project backend pytest backend/app/tests",
    "test:deploy": "bash scripts/deploy-tests/run.sh",
    "test:e2e": "npm --prefix frontend exec -- playwright test",
    "test:e2e:container": "node scripts/test-e2e-container.mjs",
    "test:scripts": "node --test scripts/*.test.mjs",
    "typecheck": "npm --prefix frontend run typecheck",
    "typecheck:backend": "uv run --project backend mypy --config-file backend/pyproject.toml backend/app",
    "verify": "npm run check && npm run test && npm run build"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.4"
  }
}
