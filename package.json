{
  "name": "full-stack-cen-template-backend-only",
  "private": true,
  "type": "module",
  "packageManager": "npm@10.9.8",
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "check": "node --test scripts/*.test.mjs && biome check scripts/*.mjs && uv run --project backend ruff check backend && uv run --project backend ruff format --check backend",
    "db:migrate": "uv run --project backend alembic -c backend/alembic.ini upgrade head",
    "db:revision": "uv run --project backend alembic -c backend/alembic.ini revision --autogenerate",
    "dev": "node scripts/dev.mjs",
    "fix": "biome check --write scripts/*.mjs && uv run --project backend ruff check --fix backend && uv run --project backend ruff format backend",
    "test": "uv run --project backend pytest backend/app/tests backend/tests",
    "verify": "npm run check && npm run test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.4"
  }
}
