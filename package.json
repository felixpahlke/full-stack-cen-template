{
  "name": "full-stack-cen-template",
  "private": true,
  "type": "module",
  "packageManager": "npm@10.9.8",
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "check": "biome check scripts/*.mjs && uv run --project backend ruff check backend && uv run --project backend ruff format --check backend",
    "fix": "biome check --write scripts/*.mjs && uv run --project backend ruff check --fix backend && uv run --project backend ruff format backend",
    "test": "node --test scripts/*.test.mjs && uv run --project backend pytest backend/app/tests backend/tests",
    "verify": "npm run check && npm test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.4"
  }
}
