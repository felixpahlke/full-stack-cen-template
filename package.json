{
  "name": "full-stack-cen-template",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.29.3",
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "build": "node scripts/build-backend-image.mjs",
    "check": "biome check scripts/*.mjs && pnpm run typecheck:backend && uv run --project backend ruff check backend && uv run --project backend ruff format --check backend",
    "deploy:ce": "./scripts/ce-deploy.sh",
    "deploy:oc": "./scripts/oc-deploy.sh",
    "dev": "node scripts/dev.mjs",
    "fix": "biome check --write scripts/*.mjs && uv run --project backend ruff check --fix backend && uv run --project backend ruff format backend",
    "test": "pnpm run test:scripts && pnpm run test:deploy && pnpm run test:backend",
    "test:backend": "cd backend && ./scripts/test.sh",
    "test:deploy": "bash scripts/deploy-tests/run.sh",
    "test:scripts": "node --test scripts/*.test.mjs",
    "typecheck:backend": "uv run --project backend mypy --config-file backend/pyproject.toml backend/app",
    "verify": "pnpm run check && pnpm run test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.4"
  }
}
